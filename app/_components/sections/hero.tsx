import { HeroContent } from "@/app/_components/sections/hero-content";

export function Hero() {
  return (
    <section id="inicio" aria-label="Apresentação">
      <HeroContent />
    </section>
  );
}
